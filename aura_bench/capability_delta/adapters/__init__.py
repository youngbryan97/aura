"""Concrete benchmark adapters.

Each module here implements ``BenchAdapter``.  Most are stubs that
document the contract for a real benchmark (SWE-bench, WebArena,
GAIA, long-horizon, adversarial) — wiring the actual eval requires
GPU/network/external repos out of scope for the harness commit.
"""
