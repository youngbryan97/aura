{
  "schema_version": 1,
  "description": "Parameters every production caller hands an empty collection. Reported by core/verify/can_this_ever_fire.py. A reviewed entry carries the reason it is right; an unreviewed one is recorded so it is not lost, and counts as outstanding. Anything not listed here is a new finding and fails the gate.",
  "non_claims": [
    "A listed entry is not approved by being listed. Only a reason approves it.",
    "This finds a mechanism nobody can switch on. A gate nobody can fail is the same defect from the other side and needs the runtime, not this."
  ],
  "reviewed": {
    "core.interiority.ledger:practice:blockers": "core/interiority/proving.py builds ledger fixtures for the proving harness, and a practice with no blockers is one of the states it is proving over. A fixture is entitled to be empty.",
    "core.connectome.gating:routing_change:baseline_state": "The empty baseline IS the comparison this caller wants: the ungated graph, so the report says how much the current state closes off. Reading it as 'no baseline' would be wrong — but it is passed explicitly rather than defaulted, which is why it reads as a switch and belongs on this list.",
    "core.learning.verified_token_trace:build_tokenizer_bundle_identity:encode_options": "The bundle identity is over the DEFAULT encode path; options that change it would change the identity, which is the point. Empty is the identity being computed.",
    "core.learning.verified_token_trace:build_tokenizer_bundle_identity:decode_options": "Same as encode_options: the identity is of the default decode path."
  },
  "recorded_not_yet_reviewed": {
    "core.brain.llm.latent_cortex.commitment_ablations:_verdict:checks": "Three call sites, all with no checks. Whether a verdict with no checks is meaningful has not been established here.",
    "core.conversational.humor_engine:observe_delivered_response:metadata": "The orchestrator tells the humor engine about every delivered response and hands it no metadata. Whether the body needs any has not been established here.",
    "core.learning.verified_transition_transaction:_load_locked:expected_events": "A load that expects no events. Whether the expectation is meant to be empty on this path has not been established here."
  }
}
