"""Deterministic engineering design: quantities, models, analyses, drawings.

The cortex decides what to build and why. Nothing in this package asks it for
a number. Every value on a finished drawing is computed here from geometry,
material properties and physical law, and the drawing refuses to render a
callout whose number has no computation behind it.
"""
