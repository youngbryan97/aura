# core/identity — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 12 outbound edges when this was generated.

description = "core.identity: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.identity",

    # What this package reaches for today.
    "+core.agency",
    "+core.brain",
    "+core.canonical",
    "+core.config",
    "+core.container",
    "+core.governance",
    "+core.governance_context",
    "+core.identity",
    "+core.organism",
    "+core.runtime",
    "+core.utils",

    # Structural invariants live next to what they protect, and reaching the
    # verifier is how they register. One way: nothing in core.verify may
    # reach back.
    "+core.verify",
    "+core.will",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
