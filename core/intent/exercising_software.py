"""Kept as the name one caller knew; the idea lives next door.

Being handed a library and asked to use it, and being handed a spreadsheet and
asked what it adds up to, are the same request: work something out from a thing
that is named rather than described. `core/intent/needs_computation.py` owns
both, because two readers of one judgement is what this tree keeps paying for.
"""

from __future__ import annotations

from core.intent.needs_computation import asks_to_exercise_software

__all__ = ["asks_to_exercise_software"]
