# core/self — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 22 outbound edges when this was generated.

description = "core.self: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.self",

    # What this package reaches for today.
    "+core.affect",
    "+core.being",
    "+core.capability_engine",
    "+core.config",
    "+core.consciousness",
    "+core.container",
    "+core.conversation",
    "+core.dialogue",
    "+core.epistemics",
    "+core.event_bus",
    "+core.intent",
    "+core.memory",
    "+core.ontogeny",
    "+core.runtime",
    "+core.security",
    "+core.self",
    "+core.self_model",
    "+core.senses",
    "+core.skills",
    "+core.soma",
    "+core.state",
    "+core.utils",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
