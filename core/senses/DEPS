# core/senses — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 27 outbound edges when this was generated.

description = "core.senses: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.senses",

    # What this package reaches for today.
    "+core.affect",
    "+core.brain",
    "+core.cognition",
    "+core.config",
    "+core.consciousness",
    "+core.constitution",
    "+core.container",
    "+core.conversation",
    "+core.event_bus",
    "+core.governance_context",
    "+core.health",
    "+core.media",
    "+core.memory",
    "+core.networking",
    "+core.orchestrator",
    "+core.perception",
    "+core.resilience",
    "+core.runtime",
    "+core.security",
    "+core.senses",
    "+core.supervisor",
    "+core.thought_stream",
    "+core.utils",
    "+core.voice",
    "+core.world_state",

    # Outside core. Each of these is a layering break already in
    # the tree; recorded here so it is visible where the rule is,
    # and so removing it is a one-line diff.
    "+interface",
    "+skills",

    # Everything else.
    "-core",
    "-aura_main",
]
