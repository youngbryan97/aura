# core/social — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 17 outbound edges when this was generated.

description = "core.social: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.social",

    # What this package reaches for today.
    "+core.agency",
    "+core.autonomy",
    "+core.brain",
    "+core.config",
    "+core.consciousness",
    "+core.container",
    "+core.epistemics",
    "+core.ethics",
    "+core.governance",
    "+core.governance_context",
    "+core.memory",
    "+core.runtime",
    "+core.security",
    "+core.senses",
    "+core.social",
    "+core.utils",

    # Invariants over the signalling and receptivity channels register with
    # the verifier from inside this package.
    "+core.verify",
    "+core.will",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
