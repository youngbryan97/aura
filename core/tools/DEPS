# core/tools — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 10 outbound edges when this was generated.

description = "core.tools: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.tools",

    # What this package reaches for today.
    "+core.agency",
    "+core.config",
    "+core.container",
    "+core.observability",
    "+core.resilience",
    "+core.runtime",
    "+core.sandbox",
    "+core.security",
    "+core.skills",
    "+core.tools",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
