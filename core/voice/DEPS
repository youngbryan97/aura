# core/voice — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 14 outbound edges when this was generated.

description = "core.voice: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.voice",

    # What this package reaches for today.
    "+core.brain",
    "+core.container",
    "+core.conversation",
    "+core.conversational",
    "+core.event_bus",
    "+core.exceptions",
    "+core.executive",
    "+core.governance_context",
    "+core.managers",
    "+core.resilience",
    "+core.runtime",
    "+core.senses",
    "+core.utils",
    "+core.voice",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
