# core/world_model — layering rules. Written, not generated.
#
# Same reason as core/consciousness. Cycle observation reads the canonical
# percept representation — `+core.state.percepts`, one module — and a
# generated rule would say `+core.state`, which is the whole state layer.
# The narrow line is the rule; regenerating deletes it.
#
# Everything else below is the closed-world list the generator wrote.

description = "core.world_model: declared dependencies only; cycle observation reads the canonical percept representation through core.state.percepts."

include_rules = [
    "+core.soma",
    "+core.world_model",

    # What this package reaches for today.
    "+core.advanced_cognition",
    "+core.brain",
    "+core.cognition",
    "+core.config",
    "+core.constitution",
    "+core.container",
    "+core.governance_context",
    "+core.health",
    "+core.resilience",
    "+core.runtime",
    "+core.state.percepts",
    "+core.thought_stream",
    "+core.values",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
