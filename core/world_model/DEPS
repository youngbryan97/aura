# core/world_model — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are what this package imports today, package by package. The
# `-core` line after them forbids the rest, so a new cross-package dependency
# is an edit to this file rather than an import nobody sees. Regenerate with
# `make deps-generate`; replace this file with a written one when this
# package earns a real architectural rule, the way core/runtime/DEPS has.
#
# 13 outbound edges when this was generated.

description = "core.world_model: may import only what it already imports; every new edge is an edit here."

include_rules = [
    "+core.world_model",

    # What this package reaches for today.
    "+core.advanced_cognition",
    "+core.brain",
    "+core.cognition",
    "+core.config",
    "+core.constitution",
    "+core.container",
    "+core.governance_context",
    "+core.health",
    "+core.resilience",
    "+core.runtime",
    "+core.thought_stream",
    "+core.values",
    "+core.world_model",

    # Everything else.
    "-core",
    "-interface",
    "-skills",
    "-aura_main",
]
