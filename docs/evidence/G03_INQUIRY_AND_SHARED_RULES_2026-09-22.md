# Discriminating inquiry and shared rule evidence

Bryan's questionnaire is mapped in
[the implementation worklist](../G_LEDGER_HUMAN_INPUT_IMPLEMENTATION.md).

The semantic portfolio now exposes `plan_inquiries`. It uses its existing
counterexample inputs, executes every hypothesis through the checked floor,
and ranks observations through the existing perception information-gain
module. Duplicate method implementations do not gain additional prior mass.
Execution errors are not treated as semantic predictions. The inquiry binds
its inputs and program identities. An independent observed value can report
compatible methods; this does not prove a program correct everywhere and
does not alter selection automatically. Desktop inquiry delivery is not yet
wired or measured.

Relational evidence now feeds the existing ProceduralGeneralizer. That shared
module had ignored failed outcomes when their resolution matched the rule
being derived. Such failures now count as contradictions. Reusing a trace
under another context cannot inflate relational support. The combined
relational, procedural, inquiry and portfolio suites pass 48 tests.

The follow-on adapter records predicted interpretations beside the existing
AgentModel prior and resolves them from observed outcomes. Goal comparison
preserves order, symbols, case and Unicode. Typed canonicalization avoids
permutations across distinct roles and retains exact results. Expanded tests
cover these contracts, with 85 passing.

Partial analogy reuses `structure_mapping`. The shared mapper now admits a
larger source against a smaller target, and excludes unmapped objects and
predicates instead of matching them by their surface names. The related
mapper and relational suites pass 77 tests before the final adapter case.

The pending scaled contextual experiment finished before further fits were
deferred in favor of implementation. On frozen source fold 0 it obtains
508/508 training operation sets and 220/256 held-out operation sets. Its
context lesion also obtains 220/256. This does not demonstrate an accuracy
benefit from cross-token communication. No full graph, fresh replication,
promotion or G03 closure follows.

Receipt:
`~/.aura/rlc-evidence/semantic-context-scaled-fold0-20260922/report.json`.
The paired 16-row pilots are retained under
`semantic-context-scaled-pilot-20260922` and
`semantic-context-unscaled-pilot-20260922`; they are feasibility measurements.
Scaling is opt-in and recorded in configuration. Existing checkpoints keep
their original projection behavior.

Follow-on verification: 153 focused tests pass across the relational,
procedural, inquiry, portfolio, contextual, partial-mapping, transfer-search
and environment suites. Recorded counterevidence now invalidates matching
rules already available for reuse without requiring a new derivation call.
Protected counterevidence retires the affected rule. Unmeasured outcomes,
failed alternatives and failures outside the rule's conditions do not
refute it. Interpretation feedback binds the original goal and prior;
external resolution cannot impersonate delivered evidence.

The portfolio measurement tool can retain answer-blind inquiry records with
`--plan-inquiries`. It plans these before reading evaluation labels and records
their cost separately. This extends Q16/Q22/Q25 in the advisory disposition;
it does not close their live-delivery or held-out acceptance obligations.
