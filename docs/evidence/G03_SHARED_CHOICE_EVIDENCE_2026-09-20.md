# Shared conditional evidence

The bounded conditional run passed the previously unbounded linear screen,
completed three source-mining rows, then exited with code 137. It produced no
fit or accuracy result. Exit 137 alone does not identify the actor or prove an
out-of-memory kill. The surviving row receipts remain under
`~/.aura/rlc-evidence/semantic-conditional-fit-bounded-20260920/`.

Inspection found a concrete resource defect: every register alternative built
another copy of the same argument feature for a given operation, mention, and
slot. Relation scoring also repeated the whole definition projection for each
selected label in one evidence bank. Conditional denominators amplified both
costs because they retain all competing options.

Argument features now share storage within a source-mining call. The cache is
discarded before another source or coefficient revision. A chart's correct and
incorrect interpretations share its complete denominator evidence. Relation
scores and gradients aggregate by evidence bank; distinct labels remain distinct
choices. Denominators with the same scale share one batched gradient calculation,
with separate softmax normalization for each local pool.

No alternative is dropped and no semantic score is intentionally changed.
Tests compare scores, gradients and directional derivatives against independent
scalar calculations, including mixed scales and shared evidence after archive
replay. The first suite passed 95 tests; after batching denominator gradients,
96 tests pass. Real source fitting remains to be measured with this storage
change. G03 stays open and no serving candidate changes.

The requested independent review did not run: the subagent reported an account
usage limit. It supplies no review evidence. The implementation checks above
were run by the parent task.
