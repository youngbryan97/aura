# G03 Span Fit Recovery

The paired span-set pilot stopped before convergence. Its detached receipt
`f63ec1d7b05d07c2446562d7e545b7a96be2dda35d9cdf559a8d78c45f17f409`
records 3,502.790978 seconds, return code -14, and an empty descendant
lineage. The script set SIGALRM at 3,500 seconds. No candidate or fit receipt
was exported. This run supplies no accuracy result.

The frozen pilot used the older pair-product calculation. The current
implementation streams pair products by span diagonal; numerical tests
compare both projections and adjoints with the original expression.

The fitter now persists accepted iterates through the shared numerical
archive writer. Resume requires identical source evidence, interval
inventory, objective parameters, and original regularization center.
L-BFGS history starts fresh; the receipt says so. Restart is not a claim of
bit-identical optimization. Export still requires optimizer convergence.

Focused checks cover interruption after an accepted iterate, restored
coefficients, unchanged regularization center, changed-objective rejection,
and corrupted-coefficient rejection. The standard refit command selects a
checkpoint beside its output, or in the explicit fit-checkpoint directory.

G03 remains open. Numerical convergence, full development evaluation,
untouched transfer evidence, and live integration are separate obligations.
