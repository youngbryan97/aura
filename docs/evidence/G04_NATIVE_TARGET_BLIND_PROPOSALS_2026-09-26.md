# Native proposal boundary

The 72/72 fresh-schema selection result used a target-derived candidate
inventory. Two subsequent development canaries removed that inventory.
Neither produced a correct program. No candidate is promoted.

## Unconstrained continuation

`tools/evaluate_semantic_native_fresh_generation.py` loaded the frozen
fold-0 step-256 adapter and used the unchanged natural request and installed
chat template, with greedy decoding and a 256-token bound. The first three
scalar requests produced zero valid typed programs: two invalid public
programs and one incomplete private-thinking boundary. This tests program
output, not the correctness of any ordinary numerical or prose answer.

- Directory: `~/.aura/rlc-evidence/semantic-native-fresh-generation-canary-20260926/`.
- Plan: `8a02890c427937ebf91448138d8194d2b939a1a8cdcab9d272b1d06a121aa80c`.
- Elapsed: 88.338 seconds.

## Typed semantic decisions

`decode_native_grammar` supplies only canonical JSON scaffolding and the
existing execution floor's type constraints. The same frozen native suffix
chooses operations, every argument register, and termination. No annotation,
target program, answer, or construction label reaches that scorer. Public
input arity and types are declared by the evaluator; their extraction from
language remains a separate unproved boundary.

The balanced canary contains one scalar, lookup, and count request. Two
graphs remained disconnected at the eight-step bound; the third completed
in three steps but was wrong. Exact-program and exact-answer scores are
both 0/3. A separate CPU process checked plan, report, and row digests,
replayed every recorded decision against the current grammar, and verified
the generated graphs and grades.

In all three cases, the second operation used input register 3 where the
target required newly computed register 4. The initial operation sequence
was right in each case. This is consistent with an arity-sensitive binding
shortcut, but three cases do not establish its cause. Whole-graph candidate
discrimination does not establish that local greedy decisions can construct
the same graph.

- Directory: `~/.aura/rlc-evidence/semantic-native-grammar-canary-v2-20260926/`.
- Plan: `2769f63c50bd1351dfcdd29155363c26690b4007e5824d90a4485c1b856f02f4`.
- Report: `6fe872aa32786d6e04708aa9db3c72be61bdba7e2d615691ce5d827370cbb9de`.
- Elapsed: 617.349 seconds.

The historical report's `bound_forced_completion=2` includes the two
disconnected failures. They were never executed or scored as successes.
The evaluator now distinguishes depth exhaustion from forced completion of
a connected graph; historical receipts are unchanged. These are exposed
development canaries, not independent transfer qualification. G03-G05 remain
open, and neither experiment changes serving or fusion authority.
