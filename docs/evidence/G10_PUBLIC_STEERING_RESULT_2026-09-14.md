# Complete public steering comparison: negative

The frozen public-channel campaign completed 216 decodes on the exact resident
27B checkpoint: nine conditions, six tasks and four trials, with alpha 0.05 and
the same 3,072-token allowance for every arm. All public answers completed.
The supervised worker ran for 8,532.20 seconds and left no descendants.

Independent replay confirms one treatment target-score win versus four matched
no-op wins. Random-vector, shuffled-layer and zero-vector controls each have
zero wins. Two task-level target-score deltas are negative. The intended
direction, adversarial comparison and specificity requirements do not pass;
`verified=false`. Successful answer completion is not a positive steering result.
This score measures the campaign's declared affect language, not consciousness,
general reasoning or independent correctness of self-reports.

The evaluated gradient-trained generation is
`462de892e4fb93d55c2a56f46968f59b9940280534319dfae5d209ebd436b87d`.
It is not the generation in the active signed steering authority,
`0d192ee439b964ba9e5792e288664835f7979743021b503434c6a3ed9dd8c51a`.
The active authority and older evidence are unchanged. The negative candidate
has not been published, fused or substituted for the active generation.

Raw result, independent replay and process receipt are retained under
`artifacts/migration/27b/recovery/public_full_20260914/`. G10 and G11 remain open.

## Follow-on probe identity

A follow-on channel probe first used the older frozen tool, which loaded the
1.5B default despite the explicit 27B path. Geometry checks rejected attachment.
The rerun used the corrected loader, but test-profile state resolution hid the
active authority and an identity rebuilt without registered repository/revision
metadata did not match the resident descriptor. It was stopped; neither attempt
provides channel evidence. Both process receipts remain in the local experiment
directories `fusion-current-generation-20260914` and
`fusion-current-generation-v2-20260914`.

The tool now rehashes the active checkpoint with its registered metadata before
loading. `--require-active-cortex` rejects missing authority or a different
checkpoint instead of measuring a derived substitute. Explicit owner-state
selection keeps verification against the real authority while logs remain
isolated. Fifty-two focused identity, ownership and certificate tests pass.

The next preflight exposed the tool's unconditional `AURA_TESTING` default:
it classified a real model-lane measurement as a hermetic test and therefore
could not own the live lane. The tool no longer changes the runtime profile.
Tests still inject their own state explicitly. The expanded 53-test suite
passes, including a fresh-process check that importing the tool does not
redirect authority. The failed v3 process made no model measurement.
