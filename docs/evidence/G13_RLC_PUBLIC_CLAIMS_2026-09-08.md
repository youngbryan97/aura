# G13 public RLC claim reconciliation

Date: 2026-09-08. Source basis: `43a4923ee`.

## Scope and evidence

This closes the public documentation obligation, not a research milestone.
The [G01 baseline](G01_RLC_BASELINE_2026-09-08.md) and its hash-bound manifest
are the numerical basis. No new inference or live activation was measured here.

Updated surfaces: `README.md`, `docs/RECURSIVE_LATENT_CORTEX.md`,
`docs/INTRINSIC_RECURRENCE.md`, and the current scope annotations in
`docs/RLC_RECONCILIATION.md`. Historical verdict artifacts remain unchanged.

The canary runner computes typed state with the semantic neural machine before
state-conditioned language-model decoding. Its coefficient lesions support
dependence on that machine within the recorded conditioning and retry contract;
they do not isolate repeated resident-transformer layers as the cause of CP566.
The guides now distinguish those mechanisms.

The 32B and 27B studies retain their 60/60 treatment results and full controls.
The 27B ordinary arm's zero parseable final answers is not represented as a
measurement of absent knowledge. Dated serving qualification is not described
as proof that the current process serves the package.

The fresh v16 natural-language replication remains a failure: 26/96 answers
against a 48/96 preregistered floor. The v19 93/96 repair is development on an
exposed cohort, not fresh replication. Bounded v14 transfer uses shared
primitives and exact execution assistance. Broad reasoning, frontier
performance and static RLC fusion remain unproven.

## Executable checks

- Seven focused baseline, public-claim and figures suites: 38 passed in 7.77 s.
- `make smoke`: 164 passed, one skipped in 54.56 s.
- `make rlc-figures writing`: passed; prose and docstring findings remain at
  their existing baselines (9 and 68).
- Ruff for the modified public-claim suite: passed.
- `git diff --check`: passed.

The public-claim regression suite obtains the failed-replication and
development fractions from the frozen manifest, verifies the evidence links,
and prevents reintroduction of the specific corrected mechanism attributions.
The baseline suite separately regrades retained outputs and verifies hashes.
These checks protect documentation accuracy; they cannot authorize deployment
or substitute for the still-open G02 through G12 obligations.
