# R03 and R04: open live checks

## Owner recovery repair

Commit `27f842d37` repairs two paths that bypassed progress-aware ownership:

- A waiting client previously evicted an old holder without checking recent
  progress. It now requires both age and silence to exceed the eviction limit.
- FlagshipDoctor previously issued an unscoped generation abort after scoped
  owner recovery, even when recovery refused to clear a progressing owner.
  It now returns on refusal and never issues the second unscoped abort.

Focused verification: 29 passed, 93 deselected, 9.02 seconds, using
`test_mlx_foreground_lease_is_measured.py` and
`test_enterprise_hardening_fixes.py`, filtered by `foreground or flagship`.
Logs and state were isolated under `/tmp/aura-r04-tests` and
`/tmp/aura-r04-state`. This establishes regression coverage, not live closure.

## R03 probe disposition

The desktop request in session `r03-live-latent`, idempotency key
`r03-live-latent-20260906`, completed in the delivery journal with HTTP 200
after 625.5 seconds. The HTTP observer timed out at 600 seconds. The durable
answer survived and was visible after reconnecting the browser.

The answer compared optimistic and pessimistic queue locking and supplied a
CAS example. This was ordinary decoding, not a latent episode: prompt
complexity alone does not admit the general latent route. It therefore does
not validate native-thinking sentence grace and cannot close R03. The answer
also compares optimistic claiming against a deliberately poor pessimistic
implementation; it is not independent proof of the performance preference.

## Remaining live evidence

- R03 needs an admitted resident latent episode that exercises public sentence
  grace without leaking private reasoning.
- R04 needs source-matched live ownership recovery evidence and completion of
  the remaining eviction-path audit.
- The neural feed showed `health_snapshot_expired` while conversation was
  available and on-loop autonomy ledger writes. These remain R07 and R06
  obligations, respectively.

Neither R03 nor R04 is marked complete by this record.
