# executors/ — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are the core packages this tree imports today. The `-core`
# after them forbids the rest, so a new dependency from here into a cognitive
# or runtime internal is an edit to this file. Regenerate with
# `make deps-generate`.
#
# 1 core packages reached when this was generated.

description = "executors: may import only the core packages it already imports."

include_rules = [
    "+core.runtime",

    # Everything else in core.
    "-core",
]
