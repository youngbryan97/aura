# skills/ — layering rules. Generated by tools/generate_deps.py.
#
# The `+` lines are the core packages this tree imports today. The `-core`
# after them forbids the rest, so a new dependency from here into a cognitive
# or runtime internal is an edit to this file. Regenerate with
# `make deps-generate`.
#
# 7 core packages reached when this was generated.

description = "skills: may import only the core packages it already imports."

include_rules = [
    "+core.config",
    "+core.container",
    "+core.governance_context",
    "+core.runtime",
    "+core.security",
    "+core.skills",
    "+core.utils",

    # Everything else in core.
    "-core",
]
